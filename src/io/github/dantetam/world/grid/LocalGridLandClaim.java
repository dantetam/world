package io.github.dantetam.world.grid;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.List;
import java.util.Set;

import io.github.dantetam.vector.Vector3i;

public class LocalGridLandClaim {

	/*
	public Set<Vector3i> allClaims;
	
	public LocalGridLandClaim() {
		allClaims = new HashSet<>();
	}

	public static class GridRectInterval {
		public Vector3i start;
		public Vector3i end;
		public GridRectInterval(Vector3i s, Vector3i e) {
			start = new Vector3i(
						Math.min(s.x, e.x),
						Math.min(s.y, e.y),
						Math.min(s.z, e.z)
					);
			end = new Vector3i(
					Math.max(s.x, e.x),
					Math.max(s.y, e.y),
					Math.max(s.z, e.z)
				);
		}
	}
	*/
	
}
